name = "gu-admissions-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Non-secret vars (safe to keep here):
[vars]
NEON_URL = "https://ep-dry-sea-aoqk565q.apirest.c-2.ap-southeast-1.aws.neon.tech/neondb/rest/v1/applications"
ALLOW_ORIGIN = "https://jaehoonjung84.github.io"

# Secrets are NOT stored here. Set them with:
#   npx wrangler secret put NEON_TOKEN
#   npx wrangler secret put STAFF_KEY
